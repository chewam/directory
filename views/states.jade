h2.page-header Trouvez des #{title} dans votre département
h4 Ou cherchez des 
    a(href="http://localhost:3001/concessionnaires/villes.html") #{title} par ville
br
div.row

    div.span8
        ul
            - for (var i = 0, l = (count / 2); i < l; i++)
                li
                    a(href="http://localhost:3001/concessionnaires/departement/"+items[i].state+","+items[i].statecode+".html")
                        | #{items[i].statecode} #{items[i].state} 
                    | #{items[i].count} #{title.toLowerCase()} trouvés

    div.span8
        ul
            - for (; i < count; i++)
                li
                    a(href="http://localhost:3001/concessionnaires/departement/"+items[i].state+","+items[i].statecode+".html")
                        | #{items[i].statecode} #{items[i].state} 
                    | #{items[i].count} #{title.toLowerCase()} trouvés
